You are an expert full-stack engineer and compiler-tooling engineer.

I am participating in a compiler-focused hackathon and need a WORKING MVP DEMO TODAY.

You will build the complete MVP application described below.

============================================================
PROJECT
============================================================

Name:

LPTA — LLVM Pass Transformation Analyzer

Short name:

LPTA

Tagline:

"Trace how LLVM transforms your program, one pass at a time."

============================================================
OFFICIAL PROBLEM STATEMENT
============================================================

LLVM applies a large sequence of optimization passes, but understanding how the complete pipeline transforms a program is difficult.

LLVM provides individual diagnostics and debugging options, but developers still lack a unified view that correlates:

1. Which pass changed the IR
2. What changed
3. How significant that change was

As a result, investigating optimization and performance regressions often requires manually collecting and comparing IR from multiple stages of the pipeline.

============================================================
OFFICIAL GOAL
============================================================

Build an LLVM-based analysis tool — the LLVM Pass Transformation Analyzer (LPTA) — that records IR state before and after each optimization pass and presents the resulting transformation history as a single, explainable view.

The tool should connect individual passes to measurable, quantified changes in the IR, making the optimization pipeline's behavior traceable rather than opaque.

============================================================
EXPECTED OUTCOME
============================================================

The tool should give compiler developers and performance engineers an explainable, evidence-backed view of LLVM's optimization pipeline:

- Which passes ran
- What they changed
- How significant each change was
- How the changes affected final code generation

This turns manual, multi-stage IR comparison into a repeatable workflow for understanding optimization behavior.

Where cross-run comparison is implemented, the tool can also help investigate performance regressions.

============================================================
IMPORTANT MVP CONSTRAINT
============================================================

This is a HACKATHON MVP.

The primary objective is:

A WORKING DEMO THAT CAN BE SHOWN TO THE HACKATHON COORDINATOR TODAY.

Do NOT attempt to reproduce all of LLVM.

Do NOT implement a compiler from scratch.

Do NOT spend hours building complex infrastructure.

Do NOT add unnecessary features.

Build the smallest technically credible application that clearly demonstrates the core LPTA concept.

============================================================
CORE PRODUCT CONCEPT
============================================================

The user provides a small C/C++ program.

The system analyzes an LLVM optimization pipeline.

The system presents:

SOURCE CODE
    ↓
INITIAL LLVM IR
    ↓
PASS 1
    ↓
PASS 2
    ↓
PASS 3
    ↓
...
    ↓
FINAL LLVM IR
    ↓
GENERATED CODE METRICS

For every optimization pass, the application should show:

- Pass name
- Whether the pass changed IR
- IR before the pass
- IR after the pass
- Added lines
- Removed lines
- Instruction count before/after
- Basic block count before/after
- IR size change
- Transformation impact
- Explanation of the observed transformation

============================================================
CRITICAL TECHNICAL REQUIREMENT
============================================================

The application MUST distinguish between:

REAL LLVM ANALYSIS

and

DEMO MODE.

If LLVM tools are available in the Replit environment, use them.

If LLVM tools are NOT available or cannot reliably provide pass-by-pass snapshots, DO NOT waste time trying to install/build a complete LLVM environment.

Instead implement a deterministic DEMO MODE using pre-generated LLVM IR snapshots.

DEMO MODE must be clearly labelled.

Never pretend that static/demo data is live LLVM execution.

The architecture should allow real LLVM execution to be plugged in later.

============================================================
TECH STACK
============================================================

Use a simple Replit-friendly architecture.

Preferred:

Frontend:
- React
- TypeScript
- Vite
- Tailwind CSS

Backend:
- Node.js / Express OR Python / FastAPI

Choose whichever is easiest and most reliable in Replit.

Avoid unnecessary complexity.

No authentication.

No database.

No Docker unless absolutely necessary.

No microservices.

No external paid APIs.

No LLM API is required for the core analyzer.

============================================================
APPLICATION STRUCTURE
============================================================

Create:

/frontend

/backend

/shared

/samples

README.md

Keep the code modular.

============================================================
MAIN USER EXPERIENCE
============================================================

The application opens directly into the LPTA analyzer.

The first screen should immediately communicate:

LPTA
LLVM Pass Transformation Analyzer

"Trace how LLVM transforms your program, one pass at a time."

At the top display a mode indicator:

● DEMO MODE

or:

● LIVE LLVM

depending on the analysis backend.

============================================================
MAIN DASHBOARD
============================================================

Create a professional developer-tool interface.

Do NOT make this look like a generic SaaS dashboard.

Visual inspiration:

- IDE
- compiler explorer
- profiling tools
- observability dashboards
- developer tooling

Use a dark interface.

Use monospace typography for source and LLVM IR.

Prioritize information density.

============================================================
LAYOUT
============================================================

HEADER
------------------------------------------------------------

LPTA

LLVM Pass Transformation Analyzer

"Trace how LLVM transforms your program, one pass at a time."

Mode:
● DEMO MODE

Buttons:

[Analyze]

[Reset]


============================================================
LEFT PANEL — SOURCE
============================================================

Title:

SOURCE CODE

Provide:

Language selector:

C
C++

Use Monaco Editor if practical.

Otherwise use a high-quality code editor.

Include sample programs.

Default sample:

int calculate(int x) {
    int unused = 100;
    int a = x * 2;
    int b = a + 0;
    return b;
}

Provide a sample selector with:

1. Constant Folding
2. Dead Code Elimination
3. Instruction Combining
4. Control Flow Simplification
5. Mixed Optimization

============================================================
MIDDLE PANEL — PIPELINE
============================================================

Title:

OPTIMIZATION PIPELINE

Display:

Source
  ↓
Initial LLVM IR
  ↓
mem2reg
  ↓
instcombine
  ↓
simplifycfg
  ↓
dce
  ↓
Final LLVM IR
  ↓
Code Generation

Each pass should be clickable.

For every pass show:

PASS NAME

Status:
CHANGED
or
NO CHANGE

Instruction delta:

42 → 38

Impact:

LOW
MEDIUM
HIGH

Clicking the pass changes the detail panel.

============================================================
RIGHT PANEL — SUMMARY
============================================================

Title:

TRANSFORMATION SUMMARY

Display:

Total Passes
6

Changed Passes
4

Instructions
42 → 31

Basic Blocks
5 → 4

IR Reduction
26.2%

Major Transformations
3

Also display a compact visualization of instruction count through the pipeline.

Example:

Initial      42
mem2reg      38
instcombine  35
simplifycfg  34
dce          31

Use actual values from the analysis dataset.

============================================================
PASS DETAIL SECTION
============================================================

When the user clicks a pass:

Display:

------------------------------------------------------------
PASS: instcombine
------------------------------------------------------------

Status:

✓ IR CHANGED

Description:

Simplifies and combines LLVM instructions using local
canonicalization and algebraic simplification.

------------------------------------------------------------

MEASURED TRANSFORMATION

Instructions:

42 → 35

Delta:

-7

Basic Blocks:

5 → 5

IR Lines:

86 → 71

Changed Lines:

12

IR Transformation Impact:

MEDIUM

------------------------------------------------------------

WHY DID THIS PASS MATTER?

Show an evidence-based explanation.

Example:

"LPTA detected a reduction of 7 LLVM instructions during
this pass. The number of basic blocks remained unchanged,
indicating that the observed transformation primarily
simplified instructions rather than changing control flow."

IMPORTANT:

Do not claim that the program became faster unless an
actual benchmark was performed.

============================================================
BEFORE / AFTER IR
============================================================

Create a large IR comparison section.

Title:

IR TRANSFORMATION

Tabs:

[Side-by-Side]
[Unified Diff]

BEFORE:

define i32 @calculate(i32 %x) {
...
}

AFTER:

define i32 @calculate(i32 %x) {
...
}

Use syntax highlighting.

Use line numbers.

Highlight:

Removed lines
Added lines
Modified lines

Allow:

- copy
- scroll
- expand

============================================================
TRANSFORMATION HISTORY
============================================================

Create a visual timeline:

INITIAL IR
   ↓
mem2reg
   ↓
instcombine
   ↓
simplifycfg
   ↓
dce
   ↓
FINAL IR

Each stage displays:

Instruction count
Basic blocks
IR lines
Change amount

Clicking a stage opens its detailed transformation.

This is one of the MOST IMPORTANT features.

============================================================
DETERMINISTIC IR ANALYSIS
============================================================

Implement a deterministic analyzer.

Given:

BEFORE LLVM IR

and

AFTER LLVM IR

calculate:

1. IR line count
2. LLVM instruction count
3. Basic block count
4. Function count
5. Global count where possible
6. Character/byte size
7. Added lines
8. Removed lines
9. Instruction delta
10. Basic block delta
11. Percentage reduction

Do NOT use an LLM to calculate these.

Do NOT hard-code metrics.

Metrics should be derived from the actual before/after IR data.

============================================================
IMPACT SCORE
============================================================

Create a transparent heuristic called:

IR Transformation Impact

It should NOT claim to predict runtime performance.

Use measurable changes.

For example:

No change:
0

Low:
small IR change

Medium:
moderate IR change

High:
large IR change

Display the underlying metrics next to the impact label.

Example:

IR TRANSFORMATION IMPACT

MEDIUM

Instructions:
42 → 35 (-16.7%)

Basic Blocks:
5 → 5

Changed Lines:
12

This is an analytical heuristic, not a performance prediction.

============================================================
EXPLAINABILITY
============================================================

Create deterministic explanations for common LLVM passes:

mem2reg
instcombine
simplifycfg
dce
constant folding
gvn
sroa
loop-unroll
loop-vectorize

For each pass show:

GENERAL PURPOSE

and:

OBSERVED CHANGE

Example:

PASS:
mem2reg

GENERAL PURPOSE:

Promotes suitable stack-based memory operations into
SSA registers.

OBSERVED CHANGE:

LPTA detected:
- reduced load/store operations
- reduced IR instructions
- same number of basic blocks

IMPORTANT:

Do not hallucinate.

If the exact reason cannot be determined:

"LLVM changed the IR during this pass, but LPTA could not
confidently classify the exact transformation."

============================================================
DEMO DATA
============================================================

Because this is a same-day MVP, create deterministic demo
analysis sessions.

Create at least 5 realistic examples.

Example 1:

Constant Folding

BEFORE:

%1 = add i32 10, 20

AFTER:

%1 = 30

Example 2:

Instruction Combining

BEFORE:

%2 = add i32 %x, 0

AFTER:

%2 = %x

Example 3:

Dead Code Elimination

BEFORE:

%unused = mul i32 %x, 100

AFTER:

instruction removed

Example 4:

Control Flow Simplification

BEFORE:

br i1 true, label %then, label %else

AFTER:

br label %then

Example 5:

Memory-to-Register Promotion

BEFORE:

%a = alloca i32
store i32 %x, ptr %a
%v = load i32, ptr %a

AFTER:

%v = %x

These examples should contain:

- realistic LLVM IR
- pass names
- before IR
- after IR
- actual computed metrics
- diff
- explanation

Do not make them look like random fake strings.

They should resemble real LLVM IR.

============================================================
REAL LLVM BACKEND
============================================================

If Replit has LLVM installed:

Detect:

clang
clang++
opt
llc
llvm-dis
llvm-as

If available:

implement:

C/C++
 ↓
clang
 ↓
LLVM IR
 ↓
optimization
 ↓
final LLVM IR

If reliable pass instrumentation is available, capture
before/after IR for individual passes.

If not available:

use DEMO MODE.

The frontend should automatically indicate:

● LIVE LLVM

or

● DEMO MODE

============================================================
CODE GENERATION
============================================================

Implement a Code Generation section.

Display:

FINAL CODEGEN

Baseline:

Assembly instructions: XX

Optimized:

Assembly instructions: YY

Delta:

-Z

Also show a small assembly diff if available.

IMPORTANT:

Do NOT claim:

"15% faster"

unless an actual benchmark has been performed.

Call this:

Generated Code Metrics

not:

Performance Improvement.

If live code generation is unavailable in Replit:

display the feature using clearly labelled deterministic
demo data.

============================================================
OPTIONAL CROSS-RUN COMPARISON
============================================================

If time permits, implement:

RUN A
vs
RUN B

Allow the user to compare two optimization pipelines.

Example:

-O2

vs

-O3

Show:

Passes unique to A
Passes unique to B
IR differences
Instruction count differences
Code generation differences

Label this:

Optimization Run Comparison

This feature is optional.

Do NOT sacrifice the core pass analyzer for this.

============================================================
API
============================================================

Create a clean API.

GET:

/api/health

POST:

/api/analyze

Request:

{
  "language": "c",
  "source": "...",
  "pipeline": "standard"
}

Response:

{
  "mode": "demo",
  "llvm_version": null,
  "pipeline": [],
  "stages": [],
  "summary": {},
  "codegen": {}
}

Each stage:

{
  "pass_name": "instcombine",
  "changed": true,
  "before_ir": "...",
  "after_ir": "...",
  "metrics": {
    "instructions_before": 42,
    "instructions_after": 35,
    "basic_blocks_before": 5,
    "basic_blocks_after": 5,
    "lines_added": 2,
    "lines_removed": 9
  },
  "impact": {
    "score": 45,
    "label": "MEDIUM"
  },
  "explanation": "..."
}

============================================================
ERROR HANDLING
============================================================

Handle:

- invalid C/C++
- empty source
- compiler errors
- missing LLVM
- analysis failure
- malformed IR
- timeout
- unavailable codegen

Show useful error messages.

Never crash the application.

============================================================
DEMO EXPERIENCE
============================================================

The application must be extremely easy to demo.

Ideal flow:

1. Open application.

2. Application shows:

LPTA
LLVM Pass Transformation Analyzer

3. Click:

Constant Folding sample

4. Click:

Analyze

5. Pipeline animates/highlights:

mem2reg
instcombine
simplifycfg
dce

6. Summary updates.

7. Click:

instcombine

8. Before/after IR appears.

9. Diff highlights removed instructions.

10. Metrics show:

42 → 35 instructions

11. Impact shows:

MEDIUM

12. Explanation appears.

13. Scroll to:

Transformation History

14. Show complete pipeline.

15. Scroll to:

Generated Code Metrics.

This entire demo should take approximately 2–3 minutes.

============================================================
IMPORTANT PRODUCT PRINCIPLES
============================================================

The application must answer four questions immediately:

QUESTION 1:

Which pass changed the IR?

QUESTION 2:

What exactly changed?

QUESTION 3:

How significant was the change?

QUESTION 4:

What happened to the final generated code?

If the UI cannot answer these questions, improve it.

============================================================
TECHNICAL HONESTY
============================================================

Never fabricate live LLVM execution.

Never claim demo data is live.

Never invent benchmark results.

Never claim an optimization made the program faster without
actually measuring runtime.

Never claim an exact compiler transformation reason unless
the evidence supports it.

Use labels:

LIVE LLVM

DEMO MODE

MEASURED

DERIVED

ESTIMATED

where appropriate.

============================================================
DESIGN REQUIREMENTS
============================================================

Make it look like a serious compiler engineering tool.

Dark theme.

Use:

- monospace code
- compact cards
- clear borders
- subtle animations
- technical typography
- pipeline visualization
- diff highlighting
- charts

Avoid:

- generic startup landing page
- giant hero section
- excessive gradients
- stock illustrations
- unnecessary marketing copy
- fake statistics
- excessive rounded cards

The interface should feel like:

"Compiler Explorer + profiling tool + IR diff viewer."

============================================================
RESPONSIVENESS
============================================================

Desktop-first.

This is primarily a hackathon developer tool.

Make it usable at:

1366x768

1920x1080

Also make it reasonably responsive for smaller screens.

============================================================
DEVELOPMENT PRIORITY
============================================================

Implement in this order:

PHASE 1:
Application shell
Source editor
Sample programs

PHASE 2:
Demo analysis dataset
Pass pipeline
Before/after IR

PHASE 3:
IR diff
Metrics

PHASE 4:
Impact score
Explanations

PHASE 5:
Transformation history

PHASE 6:
Code generation metrics

PHASE 7:
Live LLVM integration IF feasible

PHASE 8:
Final UI polish

Do not spend time on optional features until phases 1–6
work.

============================================================
VERY IMPORTANT
============================================================

At every stage prioritize:

WORKING MVP > PERFECT ARCHITECTURE

If a technically ambitious feature threatens the deadline,
implement a smaller deterministic version and clearly label
it.

The coordinator needs to see a working concept.

============================================================
FINAL ACCEPTANCE CRITERIA
============================================================

Before declaring the project complete, verify that this exact
workflow works:

OPEN APPLICATION
      ↓
SELECT SAMPLE
      ↓
CLICK ANALYZE
      ↓
SEE OPTIMIZATION PIPELINE
      ↓
SEE WHICH PASSES CHANGED IR
      ↓
CLICK PASS
      ↓
SEE BEFORE IR
      ↓
SEE AFTER IR
      ↓
SEE DIFF
      ↓
SEE QUANTIFIED METRICS
      ↓
SEE IMPACT
      ↓
SEE EXPLANATION
      ↓
SEE COMPLETE TRANSFORMATION HISTORY
      ↓
SEE FINAL CODEGEN METRICS

The application must not contain broken buttons,
placeholder text, empty panels, or console errors.

============================================================
FINAL TASK
============================================================

Start building the application now.

Do NOT ask me to design the architecture again.

Do NOT ask me what framework to use.

Make reasonable implementation decisions yourself.

First inspect the Replit environment.

Then build the MVP incrementally.

Keep the application runnable after every major stage.

When the application is complete, verify the full demo flow
and fix any issues you encounter.

The final result should be a polished, functioning LPTA MVP
that can be demonstrated to a compiler-focused hackathon
coordinator today.